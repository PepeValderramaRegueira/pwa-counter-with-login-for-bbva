import Button from './Button';

export const Default = () => (
  <Button>
    Default
  </Button>
)

export const Primary = () => (
  <Button isPrimary>
    Primary
  </Button>
);

export default {
  title: 'components/Button',
  component: Button,
}
